import React from "react"
import ReactDOM from "react-dom"
import SL_ReactFlow from "./SL_ReactFlow"

ReactDOM.render(
  <React.StrictMode>
    <SL_ReactFlow />
  </React.StrictMode>,
  document.getElementById("root")
)
